# The public entry is registered by LynxRuntimeInitializer. Keep its methods
# and the statically listed adapter types when a consuming app enables R8.
-keep class com.example.lynxcapacitormodule.** { *; }
