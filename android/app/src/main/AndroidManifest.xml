<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".LynxShellSampleApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LynxShell"
        android:usesCleartextTraffic="${usesCleartextTraffic}">

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="${amapApiKey}" />

        <!-- Sample 只负责展示如何从普通原生页面显式调用 LynxShell Module。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NativeTabDemoActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|uiMode"
            android:exported="false"
            android:label="原生 Tab 承载 Demo" />

        <activity
            android:name=".MonitoringAcceptanceActivity"
            android:exported="false"
            android:label="Lynx 监控 G1 验收" />

        <activity
            android:name=".OtaStorageInspectorActivity"
            android:exported="false"
            android:label="OTA 磁盘浏览器" />
    </application>
</manifest>
