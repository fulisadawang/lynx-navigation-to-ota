# H3 — Inspector and documentation

Objective: expose a read-only Harmony native disk Inspector from Launcher and Native Tab, then update
Harmony README/validation/path documentation.

Required display: real Store root, App ID, current/previous, leased/orphan roles, file tree, bytes and staging.

Inspector must not scan arbitrary paths, request OTA, calculate SHA, prune or mutate files.
