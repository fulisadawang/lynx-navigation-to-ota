# H4 — Verification and report

Objective: independently verify the final Harmony implementation and record platform boundaries.

Required: Harmony/static checks, three-platform static check, HAR/App build if tools exist, simulator/hdc
runtime if available, no-candidate scan, diff check, HTML/report/Obsidian sync.

Do not call static checks or a source scan a device run; keep evidence layers separate.
