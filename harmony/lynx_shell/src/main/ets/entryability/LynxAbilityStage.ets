import { AbilityStage } from '@kit.AbilityKit';

/** App 进程 Stage 入口；Lynx/ImageKnife 初始化统一由 HAR 的 RuntimeInitializer 完成。 */
export default class LynxAbilityStage extends AbilityStage {
  onCreate(): void {
  }
}
