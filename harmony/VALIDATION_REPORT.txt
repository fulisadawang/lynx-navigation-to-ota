Lynx 4.0 HarmonyOS OTA Store v2 历史验收报告（非当前 v3 结果）
日期: 2026-08-27
范围: HarmonyOS ArkTS/ArkUI；不包含 candidate/trial 状态机

实现结果
[PASS] 远程 Release 使用 apps/<8位appId>/releases/<releaseId>
[PASS] state 使用 apps/<appId>/state.json，schemaVersion=2
[PASS] 普通模式只保留 current/previous
[PASS] staging 使用 App ID 作用域
[PASS] embedded rawfile 直接返回 ArrayBuffer，不复制到 filesDir
[PASS] current/previous 读取与首屏 previous/embedded 回退链路
[PASS] statfs 可用空间预检，下载前先 prune
[PASS] Page/Tab PreparedPageBundle 持有 OtaBundleLease
[PASS] lease 释放通过 Runtime 操作队列执行 prune
[PASS] Launcher/Native Tab 可推入只读 OTA Storage Inspector
[PASS] Inspector 展示 root、App ID、current/previous、角色、文件、字节和 staging
[PASS] Harmony 代码不包含候选版本状态字段、文件或 API

验证结果
- `python3 harmony/scripts/check_harmony_shell.py --quiet`: 85 PASS / 0 WARN / 0 FAIL
- `python3 scripts/static_check.py`: 196 PASS / 0 WARN / 0 FAIL（包含 Android/iOS/Harmony 静态项）
- `assembleHar --mode module -p module=lynx_shell_kit@default --no-daemon`: BUILD SUCCESSFUL
- `assembleApp --no-daemon`: BUILD SUCCESSFUL
- `git diff --check`: 通过

运行态边界
- 已连接 HarmonyOS Pura 90 模拟器：`127.0.0.1:5555 TCP Connected localhost`。
- 由于本次运行环境访问 TEST OTA 服务时 TLS 失败，运行态使用仅 TEST 可开启的显式 `lynx_ota_mock=1`。
- 模拟器中两个 App ID 各自保留 mock-ota-v1(previous) 与 mock-ota-v2(current)，共 12 个文件；
  观察到 Native Tab 的 current lease 存活、退出后的 lease 释放、Inspector 只读刷新、删除远程文件后
  rawfile 继续渲染，以及强制停止后的冷启动 current 加载。
- 以上是 Store v2 结构与生命周期的模拟器通过证据，不是真实 OTA Server 版本内容差异证据。
- 本次 Mac 请求真实 TEST OTA 服务返回 TLS `SSL_ERROR_SYSCALL`（HTTP code 000）；当前没有物理
  Harmony 真机目标。
- 旧全局 `releases/states` 布局的历史运行记录不能证明本轮新布局。
- 待真实服务恢复并连接物理设备后执行：真实 TEST OTA、服务端版本切换、签名包、物理真机冷启动、
  Tab cache-only、主动刷新重载、Inspector 只读刷新、lease 存活/释放、删除和 previous/embedded 回退。
